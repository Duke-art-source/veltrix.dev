# Rename this file to .env and update with your MongoDB connection string
# To use a local JSON fallback instead of MongoDB, set:
# USE_FILE_DB=true
# To use local MongoDB when installed:
# MONGO_URI=mongodb://127.0.0.1:27017/veltriv
# To use MongoDB Atlas when available:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/<database>?retryWrites=true&w=majority
